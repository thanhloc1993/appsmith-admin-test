import { When, DataTable } from '@cucumber/cucumber';

When(
    `teacher searches student's name in conversation screen on Teacher App`,
    function (table: DataTable) {
        console.log('step does not implement yet');
        console.log(table);
    }
);
When(
    `teacher filters chat groups in conversation screen on Teacher App`,
    function (table: DataTable) {
        console.log(table);
        console.log('step does not implement yet');
    }
);
